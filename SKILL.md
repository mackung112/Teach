# 🛠️ AI Skill & Technical Standards (Master Prompt)

**เป้าหมาย:** ไฟล์นี้คือ "Source of Truth" ของกฎทางวิศวกรรมคอมพิวเตอร์และมาตรฐานการเขียนโค้ด React 19, Tailwind CSS v4 และการประยุกต์ใช้ชุดทักษะพิเศษเฉพาะทางในโครงการ **"ห้องเรียนครูแม็ค"**

---

## 🚨 1. กฎเหล็กทางเทคนิค (Strictly Prohibited)

* **ห้ามใช้ Vanilla JS**: ต้องใช้ React State/Ref/Hooks เท่านั้นในการควบคุมการโต้ตอบ (เว้นแต่เป็นส่วนรับ/รันโค้ด อนุญาตให้ใช้ Plugin หรือแพ็กเกจภายนอกได้)
* **ห้ามใช้ระบบเสียงทุกชนิดอย่างเด็ดขาด (Absolute Sound Prohibition Standard)**: ห้ามใส่ฟังก์ชัน `playSound`, คลื่นเสียง Web Audio API, หรือปุ่มควบคุมสลับเสียงใด ๆ ในคอมโพเนนต์อย่างถาวร 100% เพื่อความเงียบและหลีกเลี่ยงบักการโหลดทรัพยากรขณะทำงานออฟไลน์
* **ห้ามใช้ Tailwind แบบ Dynamic**: ให้ใช้ Hardcode string เต็มรูปแบบ (เช่น `text-indigo-600` ห้ามใช้ `${color}-600`) เพื่อรองรับการทำงานของ Tailwind v4
* **ห้ามแก้ไข Core Architecture**: ห้ามแก้ไขไฟล์แกนหลักระบบ ได้แก่ `App.jsx`, `LessonViewer.jsx` และ `vite.config.js` เว้นแต่จะได้รับระบุในคำสั่งอย่างชัดเจน
* **ห้ามทำ Card-in-Card**: ทุกหน้าต้องเป็นแบบไร้ขอบ (Seamless) ผสานไปกับฉากหลังตาม Immersive Full-Page Standard
* **State Management**: ห้ามใช้ Global State สำหรับควบคุมมินิเกมหรือ Simulator เด็ดขาด ให้ใช้ Local State (`useState`, `useEffect`) เสมอเพื่อรักษาความอิสระและประสิทธิภาพ

---

## 💻 2. มาตรฐานการสร้างคอมโพเนนต์ React (Component & React Standards)

### 2.1 รูปแบบคอมโพเนนต์และการตั้งชื่อ (Component Format)
* เขียนโครงสร้างในรูปแบบ:
  ```jsx
  export default function ComponentName() { ... }
  ```
* **มาตรฐานการตั้งชื่อไฟล์ตามหลักสูตร**: ตั้งชื่อไฟล์ตามวิชาและหมายเลขบทเรียนลำดับที่ 1 จากหลักสูตร (Curriculum) ในรูปแบบ `<prefix><X>_<Y>.jsx` เท่านั้น (ตัวย่อวิชาพิมพ์เล็ก เชื่อมด้วยขีดล่าง `_` และไม่มีคำอธิบายเพิ่มเติม) เช่น:
  * วิชา Python (21902-2001): หัวข้อ 1.1 ตั้งเป็น `py1_1.jsx`, หัวข้อ 3.5 ตั้งเป็น `py3_5.jsx`
  * วิชา SQL (21901-2001): หัวข้อ 1.2 ตั้งเป็น `sql1_2.jsx`, หัวข้อ 4.8 ตั้งเป็น `sql4_8.jsx`
* **ภาษาและเนื้อหา**: โค้ดตัวอย่างในวิชาต้องใช้ **Python** หรือ **MySQL** และคำอธิบายทฤษฎีต้องเป็น **ภาษาไทย** เสมอ
* **JSX Gotchas**: หากมีสัญลักษณ์ `>>>` ในโค้ด ให้ห่อด้วย `{">>>"}` เสมอ

### 2.2 โครงสร้างแบบจำลองและการลดความซ้ำซ้อน (Clean Architecture)
* **การบันทึกตำแหน่ง (Interactive Component)**: ทุกแบบจำลอง Simulator ให้สร้างเก็บไว้ที่ `\Teach\LMS-React\src\components\interactive\<วิชา>\` เสมอ เพื่อให้ระบบโหลดผ่าน Auto-Registry
* **มาตรฐานการสร้างและใช้แม่พิมพ์ร่วมกัน (OO-style Composition - Base Components)**: เมื่อสร้างบทเรียนใหม่ **ต้อง** นำเข้าและเรียกใช้งาน "Base Components" จากโฟลเดอร์ `shared/` มาประกอบการ์ดและเฟรมหลักก่อนเสมอ เพื่อลดความซ้ำซ้อนของโค้ด:
  * `SimulatorShell`: การ์ดหลักจำลองการทำงาน (รองรับ Glassmorphism + Hover animation)
  * `ConsoleScreen`: กล่องจำลอง Python/SQL Terminal กระดานดำแสดงเอาต์พุต
  * `OptionSelector`: ปุ่มตัวเลือกแบบกริด รองรับทั้งแบบ pill และ card
  * `QuizEngine`: ระบบเกมตอบคำถาม Multiple Choice ที่จัดการด่าน/สถานะการตอบแบบเบ็ดเสร็จ
  * `AmbientBackdrop`: แผงวงจรแสงเรืองแสง (Layer 1 Background Blobs)
  * `ConceptCard`: การ์ดแนวคิดพรีเมียมสีขาว พร้อมวงกลม semantic accent และ code strip
  * `SectionBlock`: กรอบไล่โทนสีล้อมรอบกลุ่ม concept cards ย่อย
  * `LogicGateSimulator`: ระบบจำลองตรรกะ boolean แบบมีปุ่มกดและแสดงผลไหลเวียนเรียลไทม์
* **ลดความซ้ำซ้อนด้วยข้อมูล (Clean Reusable Data Mapping Standard)**: แปลงกลุ่มการ์ดหรือบล็อกที่มีความคล้ายคลึงกันให้อยู่ในรูปแบบอาร์เรย์ของอ็อบเจกต์ (Array of Objects) แล้วเรนเดอร์ออกทางหน้าจออย่างไดนามิกผ่านการ `.map()` ร่วมกับ Local State เพื่อลดจำนวนโค้ดลงมากกว่า 50%
* **ตัวแปรสไตล์แบบเคลื่อนไหว (Dynamic CSS Variable Flow Standard)**: สำหรับระบบจำลองสายการไหล (Flowchart lines) ให้ประยุกต์ใช้ SVG `stroke-dasharray` ร่วมกับการตั้งค่า Inline CSS variable (เช่น `--flow-speed`) เพื่อเชื่อมโยงความเร็วแอนิเมชันกับ React State เพื่อลดการประมวลผล

---

## 🎮 3. มาตรฐาน SVG & ผังงานโต้ตอบ (Visual Flowcharts & SVG Standards)

* **มาตรฐานจุดเชื่อมต่อและผังงาน (Pure SVG Flowchart & Junction Standards)**:
  1. เส้นสายการวิ่งเชื่อมต่อทั้งหมด (`<path>`) ต้องใส่คุณสมบัติ `fill="none"` เสมอ เพื่อป้องกันบักรูปทรงปิดถมดำทึบ (Black-Filled Polygon Bug)
  2. เมื่อมีแนวกระแสหลายสายไหลมาบรรจบกัน (Merge Points) ต้องเขียนจุดเชื่อมตรรกะแบบวงกลม `<circle cx="..." cy="..." r="..." />` เสมอ โดยใช้สีน้ำเงินอินดิโกสำหรับสถานีทิศทางหลัก และสีเทาสเลตสำหรับสายเงื่อนไขย่อย
  3. กำหนดโครงสร้างลูกศรนำทิศใน `<defs>` และเรียกใช้งานผ่าน `markerEnd` โดยคัดเลือกรหัสสีลูกศรให้ตรงตามสายตรรกะ (น้ำเงิน = สายหลัก, เขียว = จริง, แดง/ส้ม = เท็จ, เทา = สายไม่เปิดใช้งาน)
* **รูปทรงแสดงผลทางหน้าจอ (ANSI Display Symbol Fix)**: การวาดสัญลักษณ์แสดงผลทางหน้าจอ (Display) ในผังงานด้วย SVG ให้ยึดรูปทรงมาตรฐานสากล คือด้านขวาแหลม (Pointed) และด้านซ้ายโค้งเว้าเข้า (Concave arc) เช่น โค้ด `path d="M 25,5 Q 10,25 25,45 L 75,45 L 95,25 L 75,5 Z"` ห้ามใช้รูปทรงอื่นที่ผิดเพี้ยนเด็ดขาด
* **แบบจำลองซ่อมแซมจุดผิดพลาด (Interactive Repair & Morph Standard)**: ในระบบอธิบายข้อผิดพลาด (Fault diagnosis) ให้สร้างแบบจำลองโต้ตอบที่มีปุ่ม **"ซ่อมแซม"** ซึ่งจะอัปเดตสลับโครงสร้าง SVG และแก้ไขตัวอย่างชุดโค้ดโปรแกรมคู่ขนานไปสู่รูปแบบที่ถูกต้อง พร้อมสร้างแอนิเมชันแฟลชนำทางด้วยสีสันเรืองแสงเพื่อประสิทธิภาพทางการเรียนรู้ขั้นสูง

---

## 📚 4. สถาปัตยกรรมหลักสูตรและข้อมูลบทเรียน (Curriculum & Page Architecture)

### 4.1 สถาปัตยกรรมบทเรียน (Page Layout Standards)
* **สถาปัตยกรรมบทเรียนย่อยเรียงซ้อนแนวดิ่ง (Vertical Stacking Page Architecture Standard)**: ในแต่ละบทเรียนหลัก (เช่น 1.1) ให้รวมทุกหัวข้อย่อยระดับที่ 2 (เช่น 1.1.1 ถึง 1.1.7) และเครื่องจำลอง (Simulator) ที่เกี่ยวข้องทั้งหมดเรียงซ้อนกันเป็นแนวดิ่งต่อเนื่องในไฟล์ Component เดียวกัน (เช่น `it1_1.jsx`) แทนการแยกเป็นหน้าหรือแท็บ เพื่อประสบการณ์การเลื่อนเรียนรู้ที่ยาวต่อเนื่อง ไม่สะดุดขาดตอน
* **เลย์เอาต์แยกบล็อกรายหัวข้อ (Separated Blocks Layout / Fluid Open-Air)**: ห้ามยุบรวมเนื้อหาลงในแท็บ (Tab Navigation) สับเปลี่ยนหน้า ให้แยกเนื้อหาออกเป็นบล็อกอิสระแบบเปิดเผยทฤษฎีเรียงตามแนวดิ่ง ปล่อยพาดหัวและตัวอักษรเนื้อหาหลักพิมพ์ทอดลงบนเลเยอร์พื้นหลังหน้าเว็บโดยตรง ไม่หุ้มตัวอักษรคำอธิบายหลักด้วยกรอบการ์ดที่ทึบหนาซ้อนกล่อง เพื่อมิติเลย์เอาต์ดูโปร่งโล่ง สะอาดตา คล้ายนิตยสารเทคโนโลยีหรือหนังสือเรียนดิจิทัลชั้นนำ

### 4.2 ข้อมูลระบบและการเขียนเนื้อหา (Content & Data Rules)
* **โครงสร้างข้อมูลบทเรียน (Data Structure Standard)**: ทุกบทเรียนในฐานข้อมูล **ต้องมี** คีย์ข้อมูลให้ครบทั้ง 6 ฟิลด์ ได้แก่ `id`, `title`, `mainTitle`, `subTitle`, `description`, และ `content` วางไว้ในอ็อบเจกต์ `lesson` เสมอ เพื่อการแสดงผลที่ StandardHeader ได้อย่างสมบูรณ์แบบ
* **มาตรฐานชื่อหัวเรื่องภาษาตำราเรียนสากล (Explicit Textbook Terminology Standard)**: หลีกเลี่ยงการใช้คำศัพท์หรือชื่อหัวเรื่องภาษาโฆษณาเชิงเปรียบเทียบที่คลุมเครือ แต่ต้องใช้คำอธิบายทางวิชาการและชื่อหัวเรื่องตรงตามตรรกะตำราเรียนสากลชัดเจนตรงตัว (เช่น "ความหมายและองค์ประกอบของระบบคอมพิวเตอร์", "หน้าที่หลักและการทำงานของระบบคอมพิวเตอร์") เพื่อให้อ่านเข้าใจได้ทันที
* **ห้ามแสดงตัวเลขลำดับย่อยเด็ดขาด**: ในทุกคอมโพเนนต์และการ์ดเนื้อหา ห้ามพิมพ์หรือแสดงตัวเลขลำดับย่อยของหัวข้อ (เช่น 4.3.1, 4.3.2) นำหน้าหัวเรื่องเป็นอันขาด ให้แสดงเฉพาะข้อความชื่อหัวข้อเปล่า ๆ เท่านั้น เพื่อความสวยงามสะอาดตา
* **ข้อกำหนดการใช้ TeacherTask (TeacherTask Props Standard)**: ห้ามเรียกใช้งานคอมโพเนนต์ `<TeacherTask />` เปล่า ๆ เด็ดขาด จะต้องส่ง Props `title` (ชื่อภารกิจ) และ `taskText` (คำอธิบายโจทย์และขั้นตอนการทำงาน) ไปด้วยเสมอเพื่อความสมบูรณ์ในการทำใบงานปลายภาคเรียน

### 4.3 ขั้นตอนการสร้างหน้าใหม่ (New Page Creation Workflow & Overwrite Policy)
1. **สร้างไฟล์ Component**: สร้างไฟล์ใน `\Teach\LMS-React\src\components\interactive\<วิชา>\` ตามรูปแบบชื่อไฟล์ `<prefix><X>_<Y>.jsx` เท่านั้น
2. **ห้ามใส่ Header**: ห้ามใส่ส่วนหัว (`<header>`, `h1`, `h2` ที่บอกชื่อเรื่อง) ลงในไฟล์ Component เนื่องจากระบบมี `StandardHeader` จัดการให้อยู่แล้ว
3. **เชื่อมโยงข้อมูล**: นำชื่อไฟล์ Component ไปใส่ในรูปแบบ `[Marker]` ในฟิลด์ `content` ของไฟล์ Database (เช่น `[py1_1]`)
4. **นโยบายเขียนทับทันที (Overwrite Policy)**: เมื่อได้รับคำสั่งปรับปรุงบทเรียนย่อยใด ๆ ให้ AI ทำการออกแบบโครงสร้างคอมโพเนนต์ใหม่ 100% ขึ้นมาเองตั้งแต่ต้น แล้วเขียนทับ (Overwrite) ลงบนไฟล์เดิมได้เลยทันที โดย**ห้ามเรียกใช้เครื่องมือเพื่อเปิดอ่านไฟล์โค้ดดั้งเดิมตัวเก่าก่อนลงมือทำอย่างเด็ดขาด** (ไม่ต้องขอคำยืนยัน และไม่ต้องพยายามรักษาโค้ดดั้งเดิมในไฟล์เก่าไว้ เพื่อหลีกเลี่ยงดีไซน์เดิมมารบกวนรูปแบบใหม่ที่ล้ำสมัยกว่า)

---

## ⚙️ 5. Specialized AI Skills & Quality Checks

* **การใช้ระบบความสามารถเสริมของ AI (Specialized AI Skills)**: กำหนดให้เรียกใช้งานชุดเครื่องมือความสามารถพิเศษ (AI Skills ตามมาตรฐาน agentskills.io) ที่ติดตั้งอยู่ในโครงการเพื่อสร้างผลลัพธ์ที่ตรงหลักเกณฑ์ดังนี้:
  * **ด้านการพัฒนา React (React 19 Core & Best Practices)**: เรียกใช้ทักษะพิเศษ `vercel-react-best-practices` เพื่อควบคุมการเขียนโค้ด React JSX, State, Hooks และการเขียน Component ให้ลื่นไหล ทันสมัย และไม่มีบั๊ก
  * **ด้านการจัดสไตล์ด้วย Tailwind (Tailwind CSS v4 Standard)**: เรียกใช้ทักษะพิเศษ `tailwind-v4-shadcn` เพื่อรองรับโครงสร้าง CSS และการกำหนดคลาสสไตล์แบบใหม่ของ Tailwind CSS v4 ได้อย่างสมบูรณ์แบบ
* **มาตรฐานการตรวจสอบและขจัด UI Anti-patterns (Impeccable Quality Assurance Standard)**: ทุกครั้งก่อนส่งมอบงานหรือตรวจสอบคุณภาพการออกแบบ โค้ดใหม่ทั้งหมดต้องปราศจากดีไซน์บกพร่องตามที่วิเคราะห์ด้วยเครื่องมือ `npx impeccable detect` ดังนี้:
  1. **ห้ามละเลยการสแกน**: บังคับรัน `npx impeccable detect --gemini <path>` ทุกครั้งเมื่อมีการแก้ไขโค้ด ก่อนที่จะทำภารกิจเสร็จสิ้น
  2. **สัญญะและโทนสี**: หลีกเลี่ยงคู่สีเกร่อของ AI เช่น สีม่วง/น้ำเงินแบบ Gradient สลับตัวอักษรเรืองแสง ยกเว้นเป็นคู่สีที่กำหนดไว้ตามวิชา
  3. **สัดส่วนแสงเงาและขอบ**: หลีกเลี่ยงการใช้ขอบหนาด้านข้างการ์ดทึบแบบดั้งเดิม `border-l-4` ในหน้าเว็บทั่วไป ให้ใช้รูปแบบกระจกใส (Glassmorphism) และขอบบางเฉียบ `border-l-[3px]` ที่ให้ความรู้สึกพรีเมียมและเรียบหรู
  4. **สีและความต่าง**: ห้ามใช้ตัวหนังสือสีเทาบนพื้นสีที่มีความอิ่มตัวสีอื่น (เช่น `text-slate-500` บน `bg-emerald-50`) เพื่อให้อ่านง่าย สบายตา
  5. **การเคลื่อนไหว**: หลีกเลี่ยงแอนิเมชันกระตุกเด้งแบบไม่จำเป็น เช่น `animate-bounce` ให้ใช้การเคลื่อนไหวที่นุ่มนวลเป็นธรรมชาติ (`ease-out`)

---

## ☁️ 6. Git & Cloud Sync

* **Repository หลัก**: `https://github.com/mackung112/Teach.git`
* **คำสั่ง "ดึงข้อมูลล่าสุด"**: หากผู้ใช้พิมพ์คำสั่งนี้ ให้ทำการล้างโฟลเดอร์และไฟล์ค้างในเครื่องที่ไม่ได้ติดตามทั้งหมดออกก่อน (ลบของเก่าทิ้ง) แล้วดึงข้อมูลจาก Server มาลงใหม่ทับเครื่องให้สะอาดหมดจดทันทีด้วยชุดคำสั่งดังนี้:
  ```bash
  git fetch origin
  git clean -fd
  git reset --hard origin/main
  ```
