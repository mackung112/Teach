# 🎯 แผนงานและติดตามภารกิจ

รายการงานปัจจุบัน เมื่อเสร็จให้ลบออกจากรายการ แล้วย้ายสรุปลงบันทึกส่งมอบ

---

## ✅ งานที่กำลังทำ

(ว่างเปล่า รอรับภารกิจใหม่)

---

## 📋 บันทึกส่งมอบ (เก็บ 3 รายการล่าสุด)

- **ปรับปรุงตำแหน่งป้ายชื่อและสัญลักษณ์ความสัมพันธ์ใน Use Case Simulator (ooad2_3.jsx) และแก้ไขหัวเรื่อง 2.3** [23/06/2026]
  - ปรับปรุงการจัดวางข้อความความสัมพันธ์ `«include»` และ `«extend»` ป้องกันการเหลื่อมซ้อนกับเส้นเชื่อมโยงและวงรี โดยจัดป้าย `«extend»` ให้อยู่กึ่งกลางเหนือเส้นเชื่อมเฉียง และแยกข้อความ `«include»` ทั้งสองเส้นออกไปซ้ายขวาด้วย `textAnchor` เพื่อให้อ่านง่าย
  - ปรับความกว้างของ System Boundary จาก 450 เป็น 265 ให้ `ยืนยัน PIN` และ `ตัวจับเวลา` อยู่นอกระบบโหวต ตรงตามแผนภาพต้นฉบับ
  - ขยับป้ายชื่อขอบเขตระบบ `ระบบโหวต` มาอยู่ตรงกลางกรอบที่ปรับขนาดใหม่
  - แก้ไขข้อมูลหลักสูตรใน `ooadCourse.js` ให้หัวข้อวิชา 2.3 เปลี่ยนจาก "แผนภาพคลาส (Class Diagram)" เป็น "แผนภาพยูสเคส (Use Case Diagram)" เพื่อให้ชื่อเรื่องสอดคล้องตรงกับเนื้อหาตัวจำลองจริงตามที่คุณครูแจ้ง
  - ปรับปรุงเนื้อหาคำอธิบายความสัมพันธ์ Extend และ Include ในไฟล์ `ooad2_3.jsx` (ทั้งกล่องคำบรรยายขั้นตอน 1-7 และการ์ดสรุปไทม์ไลน์) เพื่อเน้นย้ำความแตกต่าง: **Extend คือเกิดหรือไม่เกิดก็ได้ (มีสถานะเป็นทางเลือก)** และ **Include คือเกิดขึ้นแน่นอนเสมอ (ต้องเกิดแน่ๆ)**
  - สแกนตรวจสอบคุณภาพด้วย `impeccable detect` และทดสอบคอมไพล์ผ่าน 100%

- **สร้าง Use Case Diagram Simulator ในบทเรียน 2.3** [23/06/2026]
  - พัฒนาเครื่องมือจำลอง Use Case Diagram Simulator แบบตอบโต้ได้ 7 ขั้นตอน ครอบคลุมการทำงานตามหลักวิชาการ
  - จัดโครงสร้างให้ Canvas SVG อยู่ตรงกลาง, ปุ่มตัวเลือกขั้นตอน 1-7 และปุ่มควบคุมอยู่ด้านล่าง Canvas
  - เพิ่มแอนิเมชัน Bounce (Spring) ให้กับกล่องแสดงรายละเอียดขั้นตอนด้านล่างสุด
  - ตรวจแก้ความประณีตสีสันและคู่ขอบของไฟล์ `ooad2_3.jsx` ทั้งหมดตามเกณฑ์ npx impeccable detect และทดสอบ build ผ่าน 100%

- **ปรับปรุงการจัดวาง Simulator บทเรียน 2.4 (Sequence Diagram)** [23/06/2026]
  - ปรับปรุงการจัดวางเลย์เอาต์ของ Simulator ใหม่ทั้งหมด (Canvas อยู่ตรงกลาง, ปุ่มตัวเลือกขั้นตอน 1-10 อยู่ด้านล่าง)
  - พัฒนาโครงสร้างการวาด SVG ให้ตรงตามภาพหน้าจอล็อกอินภาษาไทยจริง (User -> Login page -> API) มีเลขขั้นตอน 1-7 กำกับ
  - วาดแถบทำงาน (Activation Bar) ซ้อนทับเหลื่อมกัน และแขวนโน้ต Note JSON Response อย่างประณีตตามหลักวิศวกรรมซอฟต์แวร์
  - พัฒนากล่องแสดงรายละเอียดขั้นตอนด้านล่างสุดให้เด้งขึ้นแบบนุ่มนวล (Spring-like pop transition) ทุกครั้งที่ขยับขั้นตอน
  - เคลียร์ข้อผิดพลาด JSX tag mismatch ในไฟล์ `ooad2_4.jsx` ทำให้คอมไพล์ผ่าน 100%
  - ทดสอบรัน `npx impeccable detect` ผ่านเกณฑ์มาตรฐานดีไซน์ 100% ปลอดภัยไร้ที่ติ
  - เชื่อมต่อกับ GitBook MCP Server เพื่อดึงข้อมูลสำหรับบทเรียนมาเซฟไว้ที่ `ooad_lesson_2_4_content.md`
  - ปรับดีไซน์ `ooad2_4.jsx` ใหม่ทั้งหมดให้กลมกลืนเป็นไทม์ไลน์ 10 ขั้นตอนตามมาตรฐานเดียวกับหน้า 2.2 และ 2.3
  - ดึงรูปภาพประกอบที่ถูกต้องจากระบบจัดเก็บทรัพย์สินของ Saladpuk มาจัดแสดงผ่านเอฟเฟกต์โฮเวอร์ขยาย
  - พัฒนาโปรแกรมจำลองจำลองข้อมูล (Sequence Diagram Simulator) อเนกประสงค์แบบตอบโต้ 10 ขั้นตอนสวยงามในธีมดาร์กโหมด
  - ปรับโครงสร้างขั้นตอนของตัวจำลองให้ตรงกับกระบวนการประกอบร่างแผนภาพ 10 ขั้นตอนตามภาพจริงในบทเรียน (มี 3 ออบเจกต์หลัก: User, LoginPage, AuthAPI)
  - ปรับปรุงเส้นเชื่อมโยง (Message Line) และตำแหน่งของแถบทำงาน (Activation Bar) ในตัวจำลองสถานการณ์ให้ตรงและอยู่กึ่งกลาง 100% ตามมาตรฐานดีไซน์
  - สแกนตรวจสอบคุณภาพด้วย `impeccable detect` และทดสอบคอมไพล์ระบบ `npm run build` ผ่าน 100% ปลอดไร้ที่ติ

- **ปรับปรุงเนื้อหา 21910-1003/lesson/4.1 (การแสดงผลด้วย print)** [21/06/2026]
  - ปรับปรุงเฉพาะโจทย์แบบฝึกหัด (Instructor Task) ของบทเรียน 4.1 ให้มีหัวข้อโจทย์จำนวน 20 ข้ออย่างสมบูรณ์แบบ
  - เพิ่มระบบล็อกรหัสผ่าน (Passcode PIN: 1122) สำหรับเข้าดูแบบฝึกหัดท้ายบทเรียน เลียนแบบพฤติกรรมจากบทที่ 3.2
  - รักษาโครงสร้าง Simulator, Playground และมินิเกมจุดอื่น ๆ ในหน้านี้ไว้ตามเดิม 100% ตามคำแนะนำของผู้ใช้งาน

- **ปรับปรุงเนื้อหา 21910-1003/lesson/4.2 (การรับข้อมูลด้วย input)** [21/06/2026]
  - ปรับปรุงเฉพาะโจทย์แบบฝึกหัด (Instructor Task) ของบทเรียน 4.2 ให้มีหัวข้อโจทย์จำนวน 20 ข้ออย่างสมบูรณ์แบบ
  - เพิ่มระบบล็อกรหัสผ่าน (Passcode PIN: 1122) สำหรับเข้าดูแบบฝึกหัดท้ายบทเรียน เลียนแบบพฤติกรรมจากบทที่ 3.2
  - รักษาโครงสร้าง Simulator, Terminal Sandbox และมินิเกมจุดอื่น ๆ ในหน้านี้ไว้ตามเดิม 100% ตามคำแนะนำของผู้ใช้งาน

- **สร้างเนื้อหา 21910-1013/lesson/2.1 (OOP Simulator)** [19/06/2026]
  - สร้างไฟล์ `public/lessons/oop/2.1.html` เก็บเนื้อหา HTML และ Simulator แบบไม่ต้องแก้ไข (Raw HTML)
  - สร้างไฟล์ `src/components/interactive/oop/oop2_1.jsx` ซึ่งทำการครอบ HTML ไว้ใน iframe เต็มจอ
  - ปรับปรุง `LessonViewer.jsx` ให้รองรับการซ่อนส่วนหัว (StandardHeader) ผ่าน `hideHeader: true`
  - ปรับปรุงข้อมูลใน `oopCourse.js` ให้แสดงเนื้อหาผ่าน component ใหม่และซ่อนส่วนหัว
